name: Audit Firmware

on: [push]

jobs:
  compile:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./firmware

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: false

      - name: Audit
        run: |
          make audit
